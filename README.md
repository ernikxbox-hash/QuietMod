# Quiet Mod 👁️

Telegram Business бот — перехватывает удалённые и изменённые сообщения. Чёрный люкс.

## Возможности

- 🕵️ **Перехват удалённых и изменённых сообщений** (Business API) — безлимит
- 🎤 **Расшифровка голосовых** (Groq Whisper) с кнопкой «Тык»
- 🤖 **ИИ-консьерж без лимитов** (GPT-OSS 120B) — со встроенным поиском: курс валют и крипты, погода, новости, веб
- 🧾 **Готовые файлы от ИИ** — «сделай калькулятор на python» → бот пришлёт настоящий `calculator.py`; поддерживаются проекты из нескольких файлов и любые языки (`.py` `.js` `.html` `.go` `.sh` …)
- 💤 **AFK-автоответ** — включается прямо в ЛС с ботом
- 🔇 **Mute** — удалять сообщения собеседника в личке
- 🛡️ **Nomute** — если собеседник удаляет твои сообщения, бот дублирует их от своего имени (подтверждение — от имени бота в чате)
- 🛡️ **WBL-фильтр** — удаляет мат, обфускацию и флуд от собеседника
- 💻 **Режим кода** — всё, что пишешь, оформляется как код
- 🔍 **Поиск внутри `.ai`** — отдельной команды `.search` больше нет: курс валют и крипты, погода, новости и веб-поиск отвечает прямо `.ai`
- 💰 **`.price`** — оценка стоимости юзернейма (как на Fragment)
- 💱 **`.curs`** — курс популярных валют к рублю одним сообщением (официальные курсы ЦБ РФ)
- ⚔️ **`.knb`** — камень-ножницы-бумага: в ЛС с собеседником и в группах по вызову (`.knb @user` → кнопка «⚔️ Принять бой»), со счётом на реваншах
- 🖼 **`.ramka`** — золотая орнаментальная рамка на фото: ответь на чьё-то фото и напиши `.ramka`, бот вернёт фото в рамке (в ЛС с ботом: `.ramka` → пришли фото)
- 🏷 **`.stik`** — делает стикер из фото: ответь на чьё-то фото и напиши `.stik`, бот превратит его в стикер (в ЛС с ботом: `.stik` → пришли фото)
- 🎥 **`.krom`** — превращает видео в кружок: ответь на видео и напиши `.krom`, бот вернёт кружок (квадрат 640×640, до 60 сек; в ЛС с ботом: `.krom` → пришли видео)
- ✏️ **Форматирование**: `.bold` `.italic` `.mono` `.line` `.crossed` `.hidden` `.quote`
- 📚 **Архив безлимитный** — для всех, навсегда
- ◐ **Поиск по архиву** — включён для всех
- 📢 **Рассылки** админом: всем пользователям или по группам/каналам
- 🔔 **Гейт подписки** — доступ к боту (ЛС и автоматизация) только для подписчиков канала: кнопка «Подписаться» + проверка подписки
- ⟡ **Поддержка развития** — добровольный вклад 15/30/50⭐ (бот бесплатен и без лимитов)

## Команды

### В ЛС с ботом

| Команда | Что делает |
|---|---|
| `/start` | Гейт подписки → главное меню (доступ открывается после подписки на канал и кнопки «Проверить»)
| `.afk` | Включить AFK — все, кто пишет в твоих бизнес-чатах, получат автоответ «я не в сети» (в подтверждении — кнопка «🔴 Выключить AFK») |
| `.afk текст` | AFK с заметкой — она приложится к автоответу (пример: `.afk вернусь через час`) |
| `.unafk` | Выключить AFK (сбрасывает AFK и во всех бизнес-чатах) |
| `.spam текст N` / `.spam stop` | N одинаковых сообщений в текущий чат |
| 🖼 `.ramka` | Пришли фото — бот наденет золотую рамку (или сразу ответь на фото и напиши `.ramka`) |
| 🏷 `.stik` | Пришли фото — бот сделает стикер (или сразу ответь на фото и напиши `.stik`) |
| 🎥 `.krom` | Пришли видео — бот сделает кружок (или сразу ответь на видео и напиши `.krom`) |

### В бизнес-чатах (инлайн-команды, пишутся прямо в чате)

| Команда | Что делает |
|---|---|
| `.afk [заметка]` | Автоответ «я не в сети» (для этого чата) |
| `.mute` / `.unmute` | Удалять все сообщения собеседника |
| `.nomute` / `.unnomute` | Дублирует твои сообщения от имени бота — если собеседник удаляет их, копия останется (подтверждение бот присылает в чат от своего имени) |
| `.wbl` / `.unwbl` | Фильтр мата и флуда |
| `.code` / `.uncode` | Режим кода |
| `.ai вопрос` | ИИ-ответ со встроенным поиском: курс валют (`.ai курс доллара`), погода (`.ai погода в Москве`), новости, веб; работает с текстом и фото; по запросу присылает готовые файлы (`.ai сделай калькулятор на python` → `calculator.py`) |
| `.price @username` | Оценка юзернейма (без аргумента — оценит собеседника) |
| ℹ️ `.info` | Карточка собеседника: имя, username, ID, bio, Premium, настройки приватности, фото в профиле и примерную дату регистрации |
| 💱 `.curs` | Курс популярных валют к рублю — одним сообщением (официальные курсы ЦБ РФ) |
| ⚔️ `.knb` | Камень-ножницы-бумага с собеседником: секретные ходы, случайный первый ход, счёт на реваншах |
| 🖼 `.ramka` (ответь на фото) | Золотая орнаментальная рамка на фото — ответь на чьё-то фото и напиши `.ramka` |
| 🏷 `.stik` (ответь на фото) | Стикер из фото — ответь на чьё-то фото и напиши `.stik` |
| 🎥 `.krom` (ответь на видео) | Кружок из видео — ответь на чьё-то видео и напиши `.krom` |
| `.bold/.italic/.mono/.line/.crossed/.hidden/.quote текст` | Форматирование |
| `.spam текст N` / `.spam stop` | Рассылка (с кнопкой «⛔ Остановить») |

> В группах и каналах работают: `.ai` (с поиском, курсом валют и погодой), `.price`, `.info` (ответь на сообщение или укажи @username), `.curs`, форматирование, `.spam`, 🖼 `.ramka` (ответь на фото), 🏷 `.stik` (ответь на фото), 🎥 `.krom` (ответь на видео) и ⚔️ `.knb @user` (вызов на бой 1×1 с кнопкой «Принять бой»).

## Структура

```
bot.py               # точка входа: init БД, синк чатов, запуск polling, graceful shutdown
core.py              # конфигурация: env-переменные, bot/dp, логгер, FSM-состояния
database.py          # слой данных (aiosqlite + SQLite): 7 таблиц, батчеры записи, лимиты архива
business_api.py      # HTTP-обёртки Business-методов Telegram Bot API + кэш владельца connection
functions.py         # общая логика: ИИ (Groq + ротация ключей), поиск, погода, WBL-фильтр, клавиатуры
handlers.py          # регистратор хендлеров: порядок импортов = приоритет в aiogram
handlers_gate.py     # 🛡 гейт подписки: проверка getChatMember + middleware (ЛС и автоматизация)
handlers_start.py    # /start, /admin, business_connection (предзагрузка владельца)
handlers_commands.py # инлайн-команды: .spam .mute .nomute .afk .code .wbl .ai .price .curs + форматирование
handlers_games.py    # ⚔️ Камень·Ножницы·Бумага (.knb)
handlers_ramka.py    # 🖼 .ramka — золотая рамка на фото
handlers_stik.py     # 🏷 .stik — фото → стикер
handlers_krom.py     # 🎥 .krom — видео → кружок
handlers_intercept.py# ядро: перехват удалённых/изменённых сообщений + расшифровка голосовых
handlers_ai.py       # ИИ-консьерж в ЛС и поиск по архиву
handlers_menu.py     # ЛС-меню: сохранённые, howto, приглашения, профиль, архив, донаты
handlers_admin.py    # админ-панель, .cmd список функций, рассылки, группы/каналы
tasks.py             # фоновая очистка просроченных сохранённых сообщений (каждые 6 ч)
mtproto_resolver.py  # (опция) MTProto-слой: резолв юзернеймов для .sled/.info
requirements.txt
Procfile             # Railway / Heroku
railway.toml         # Railway: volume для data/bot.db
.env.example         # переменные окружения (создай по таблице ниже)
```

> ⚠️ Порядок импортов в `handlers.py` менять нельзя — в aiogram «первый подходящий хендлер выигрывает», и модуль перехвата (`handlers_intercept.py`) должен регистрироваться последним из бизнес-обработчиков, чтобы не съедать инлайн-команды.

## Деплой на Railway

### 1. Подготовка

```bash
# Залей проект на GitHub (приватный репо)
git init
git add .
git commit -m "init"
git remote add origin https://github.com/ТВОЙuser/quiet-mod-bot.git
git push -u origin main
```

### 2. Railway

1. Зайди на [railway.app](https://railway.app)
2. New Project → Deploy from GitHub repo → выбери репо
3. Перейди в Variables → добавь:

| Key | Value |
|-----|-------|
| `BOT_TOKEN` | токен от @BotFather |
| `ADMIN_ID` | твой Telegram ID |
| `GROQ_API_KEY` | ключ от [console.groq.com](https://console.groq.com) |
| `GROQ_API_KEY2` | второй ключ от Groq (запасной, необязательно) — если на первом лимит/ошибка, бот переключится на второй |
| `RAMKA_URL` | (необязательно) прямая ссылка на PNG-рамку для `.ramka` с прозрачным отверстием (например, `https://raw.githubusercontent.com/ник/репо/main/ramka.png`). Бот скачает её и наденет на фото; пусто → рамка рисуется кодом. ⚠️ Каноническое имя — капсом: `RAMKA_URL` (код также подхватит `Ramka_URL` и другие варианты регистра) |
| `CHANNEL_USERNAME` | (по умолчанию `Official_QuietMod`) юзернейм канала **без `@`** — подписка на него обязательна для доступа к боту. Пустая строка = гейт выключен |
| `CHANNEL_URL` | (по умолчанию `https://t.me/Official_QuietMod`) ссылка, на которую ведёт кнопка «🔔 Подписаться на канал» |
| `CHANNEL_ID` | (необязательно) числовой ID **правильного** канала (того, на который ведёт кнопка `CHANNEL_URL`). ⚠️ Не бери ID из старых логов — это может быть другой канал с тем же названием! Узнать ID нужного канала: добавить бота админом именно в него → в свежих логах `Бот добавлен в channel … (ID: …)`. Можно вообще не ставить: бот сам зафиксирует ID, как только его добавят в канал с юзернеймом `CHANNEL_USERNAME` |

| `TELEGRAM_API_ID` | (необязательно) api_id из [my.telegram.org](https://my.telegram.org) → API development tools. Включает **MTProto-слой**: резолв ЛЮБОГО публичного юзера по `@username` в `.sled`/`.info` (Bot API это не умеет). Без него `@username` ищется по базе бота |
| `TELEGRAM_API_HASH` | (необязательно) api_hash из my.telegram.org — пара к `TELEGRAM_API_ID` |
| `TELEGRAM_USER_SESSION` | (необязательно, **рекомендуется для `.sled`/`.info`**) `StringSession` владельца от Telethon — резолв любых публичных юзеров по `@username`, даже тех, кого бот никогда не видел (с бот-токеном MTProto видит только то, что видит бот). Как получить: `pip install telethon`, залогиниться (QR/телефон) и сохранить `StringSession(...).save()`. Строка секретная — храни только в Railway Variables |

> `BOT_USERNAME` захардкожен в `core.py` — из переменной окружения не читается.

> 🔔 **Гейт подписки: бота нужно добавить в канал один раз** — числовой ID канала сохраняется в базе, и после рестарта бот его помнит (заново добавлять не нужно). Проверка подписки работает, пока бот в канале; в логах при старте появится `🛡 Гейт подписки: @… — бот в канале (member)` или предупреждение `бот НЕ найден в канале…` (в этом случае админу также приходит уведомление в ЛС с инструкцией). Гейт закрывает ЛС с ботом и бизнес-автоматизацию; группы/каналы не блокируются.

> ⚠️ **`RAMKA_URL`: как понять, что рамка из PNG работает.** В логах бота (Railway → Deployments → Logs) при старте появится строка `🖼 RAMKA_URL задана` или `🖼 RAMKA_URL не задана`. При первом `.ramka` — `🖼 PNG-рамка загружена (…, отверстие: найдено/НЕТ)`. Если в PNG **нет прозрачного отверстия по центру** — бот нарисует рамку кодом (фолбэк), так что у рамки обязательно должен быть прозрачный центр (или репозиторий публичный, иначе скачивание не пройдёт).

4. Volumes → Add Volume → Mount path: `/app/data`
   (это сохраняет БД между перезапусками)

5. Deploy → бот запустится автоматически

### 3. Проверка

Напиши `/start` боту — должно прийти приветствие.
Администратору придёт сообщение `✔ Бот запущен`.

## Поддержка проекта

Quiet Mod **бесплатен для всех — без лимитов, платных подписок и VIP. Навсегда.**

Мы никого ни о чём не просим. Но если у тебя есть немного лишнего —
небольшой добровольный вклад очень поможет развитию:

| Вклад | На что идёт |
|-------|-------------|
| 15 ⭐ | Серверы, ИИ, развитие |
| 30 ⭐ | Серверы, ИИ, развитие |
| 50 ⭐ | Серверы, ИИ, развитие |

Звёзды отправляются через меню бота → **⟡ Поддержать проект**.
Каждый вклад — это стабильность работы 24/7 и новые возможности для всех.
Спасибо, что ты с нами 👁️

## Как работает гейт подписки 🔔

1. Пользователь нажимает `/start` → если не подписан, видит экран с двумя кнопками: **«🔔 Подписаться на канал»** (ведёт на канал) и **«✅ Проверить подписку»**
2. Вступает в канал, возвращается в бота и жмёт «Проверить» → бот проверяет через `getChatMember` и открывает главное меню
3. Каждое действие проверяется заново через Telegram API (кэша нет): вышел из канала — следующее же сообщение блокируется. Без подписки бот недоступен ни в ЛС, ни в автоматизации чатов
4. Если владелец бизнес-подключения не подписан — автоматизация (перехват, архив, уведомления) приостановлена: сообщения не сохраняются, владельцу уходит напоминание
5. Ошибки API (бот не в канале, сеть) не блокируют бота — гейт временно открывается, в логах предупреждение
6. Админ-диагностика: напиши боту **`.gate`** — статус канала и бота в нём; **`.gate <ID>`** — живая проверка подписки конкретного юзера

## Как работает бизнес-перехват

1. Пользователь подключает бота в Telegram Business → Автоматизация чатов (работает только при активной подписке на канал)
2. Все входящие сообщения проходят через `@dp.business_message()` → сохраняются в SQLite (батчером, архив безлимитен для всех)
3. При удалении — `@dp.deleted_business_messages()` → бот достаёт из БД и отправляет владельцу (медиа пересылает, голосовые расшифровывает)
4. При редактировании — `@dp.edited_business_message()` → уведомление «Было / Стало»
5. Если владелец включил AFK (в ЛС с ботом или в самом чате) — собеседникам отправляется автоответ «Я сейчас не в сети.» (+ заметка)

## Как работает AFK из ЛС с ботом

1. Напиши боту в личку: `.afk` или `.afk ушёл по делам`
2. Все, кто тебе пишет в подключённых бизнес-чатах, получат автоответ
3. Чтобы выключить: напиши `.unafk` (или `.unafk` прямо в бизнес-чате)

> AFK из ЛС действует на **все** бизнес-чаты сразу. AFK, включённый в конкретном чате (`.afk` там), действует только на этот чат.

## Как играть в ⚔️ `.knb`

**В ЛС (Business):** напиши `.knb` в чате с собеседником — игра начнётся сразу: секретные ходы, случайный первый ход, реванши.

**В группе:** напиши `.knb @ник_соперника` — бот покажет вызов, а соперник примет бой кнопкой **«⚔️ Принять бой»**. Играют 1×1, все видят, но ходы остаются секретными (соперник видит только «сделал ход 🤫»).

- Кнопки хода: ✊ ✌️ 🖐
- Кто ходит первым — решает случай, каждый раунд заново
- На реваншах ведётся **счёт**: `@ты 3:1 @соперник`
- В группе бот должен быть **админом**, чтобы видеть сообщения участников
